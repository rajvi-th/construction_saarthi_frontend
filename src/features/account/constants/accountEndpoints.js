/**
 * Account API Endpoints
 * Centralized endpoint configuration for account feature
 */

export const ACCOUNT_ENDPOINTS = {
  USER_PROFILE: '/user/profile',
};

// Flattened endpoints for easier access
export const ACCOUNT_ENDPOINTS_FLAT = {
  USER_PROFILE: ACCOUNT_ENDPOINTS.USER_PROFILE,
};

